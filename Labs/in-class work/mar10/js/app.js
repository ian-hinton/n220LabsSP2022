let txtName = document.getElementById("txtName");


function remove() {
    let userTxt = txtName.value;
    let char = userTxt[i];
    let word;
    let none;
    let some;

    for (var i = 0; i < userTxt.length; i++) {
        if (char == "a", "e", "i", "o", "u", "y") {
            word = none;
        } else {
            word = some;
        }
    }
    console.log(some);

}