let txtName = document.getElementById("txtName");


function remove() {
    let userTxt = txtName.value;
    let char = userTxt[i];
    let word;
    let vowel;
    let conson;

    for (var i = 0; i < userTxt.length; i++) {
        if (char == "a", "e", "i", "o", "u", "y") {
            word = vowel;
        } else {
            word = conson;
        }
    }
    console.log(conson);

}