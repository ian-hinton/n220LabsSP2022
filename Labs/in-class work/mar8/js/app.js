let txtName = document.getElementById("txtName");

function doubles() {
    let userName = txtName.value;
    let doubled;
    //userName += userName + userName;
    doubled = userName + userName;
    console.log(doubled);
}